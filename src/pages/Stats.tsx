import {useEffect,useState} from 'react';import {BarChart3,Heart,MessageCircle,BookHeart,CalendarDays,CheckCircle2,Mail,Camera,Plane,Target} from 'lucide-react';import {api,errorMessage} from '../lib/api';
export default function Stats(){const [data,setData]=useState<any>(null),[error,setError]=useState('');useEffect(()=>{api.get('/stats').then(r=>setData(r.data)).catch(e=>setError(errorMessage(e)))},[]);if(error)return <div className="alert-error">{error}</div>;if(!data)return <div className="card py-16 text-center"><div className="heart-loader">♥</div><p className="mt-3 text-sm text-slate-400">Counting your little world…</p></div>;const t=data.totals;const cards=[['Memories',t.memoryCount,Heart],['Dates',t.dateCount,CalendarDays],['Messages',t.messageCount,MessageCircle],['Letters',t.letterCount,Mail],['Reactions',t.reactionCount,Heart],['Comments',t.commentCount,MessageCircle],['Photos',t.memoryCount,Camera],['Bucket list',`${t.bucketDone}/${t.bucketCount}`,Target]];return <div><p className="eyebrow">A LITTLE LOOK BACK</p><h1 className="font-display text-4xl font-bold">Our relationship stats</h1><p className="mt-2 text-slate-500">Private analytics for the moments, messages and adventures you share.</p><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,Icon]:any,i)=><div key={label} className="stat-card"><span className={`icon-square tone-${i%4}`}><Icon size={19}/></span><span><span className="block text-xs font-semibold text-slate-400">{label}</span><strong className="mt-1 block text-2xl">{value}</strong></span></div>)}</div><section className="card mt-6"><div className="flex items-center gap-3"><div className="icon-square tone-3"><BarChart3 size={19}/></div><div><h2 className="font-display text-2xl font-bold">Your year together</h2><p className="text-xs text-slate-400">Monthly memory activity.</p></div></div><div className="mt-7 space-y-3">{(data.monthly||[]).map((m:any)=><div key={m._id} className="flex items-center gap-3"><span className="w-20 text-xs font-semibold text-slate-400">{m._id}</span><div className="h-8 flex-1 overflow-hidden rounded-xl bg-rose-50"><div className="h-full rounded-xl bg-rose-300" style={{width:`${Math.max(8,Math.min(100,m.count*10))}%`}}/></div><b className="w-8 text-right">{m.count}</b></div>)}</div></section><section className="card mt-6"><div className="flex items-center gap-3"><div className="icon-square tone-0"><Heart fill="currentColor"/></div><div><h2 className="font-display text-2xl font-bold">By partner</h2><p className="text-xs text-slate-400">A friendly look at each person's contributions — not a competition.</p></div></div><div className="mt-6 grid gap-4 md:grid-cols-2">{data.byPartner.map((p:any)=><div key={p.userId} className="rounded-3xl bg-slate-50 p-5"><div className="flex items-center gap-3">{p.profilePicture?<img src={p.profilePicture} className="size-12 rounded-2xl object-cover"/>:<div className="grid size-12 place-items-center rounded-2xl bg-rose-50 text-rose-400"><Heart fill="currentColor"/></div>}<div><p className="font-semibold">{p.name}</p><p className="text-xs text-slate-400">Their contributions</p></div></div><div className="mt-5 grid grid-cols-3 gap-2 text-center"><Metric label="Moments" value={p.moments}/><Metric label="Journal" value={p.journal}/><Metric label="Messages" value={p.messages}/></div></div>)}</div></section><section className="card mt-6"><div className="flex items-center justify-between"><div><h2 className="font-display text-2xl font-bold">Trip spending</h2><p className="text-xs text-slate-400">Private shared expenses across your trips.</p></div><Plane className="text-violet-500"/></div><p className="mt-5 text-3xl font-bold">Rs. {Number(t.expenseTotal||0).toLocaleString()}</p><p className="mt-1 text-sm text-slate-400">{t.expenseCount||0} expense entries</p></section></div>}
function Metric({label,value}:{label:string;value:number}){return <div className="rounded-2xl bg-white p-3"><strong className="block text-xl">{value}</strong><span className="text-[11px] text-slate-400">{label}</span></div>}
