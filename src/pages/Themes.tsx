import {useEffect,useState} from 'react';import {Palette,Sparkles} from 'lucide-react';
const themes=[['rose','Rose Nest',''],['lavender','Lavender Dream','theme-lavender'],['midnight','Midnight Date','theme-midnight'],['sage','Sage Garden','theme-sage']];
export default function Themes(){const [theme,setTheme]=useState(localStorage.getItem('couplenest-theme')||'rose'),[motion,setMotion]=useState(localStorage.getItem('couplenest-motion')!=='off');useEffect(()=>{document.documentElement.dataset.theme=theme;localStorage.setItem('couplenest-theme',theme)},[theme]);useEffect(()=>{document.documentElement.dataset.motion=motion?'on':'off';localStorage.setItem('couplenest-motion',motion?'on':'off')},[motion]);return <div><p className="eyebrow">PHASE 4 · PERSONALIZE</p><h1 className="font-display text-4xl font-bold">Themes & motion</h1><p className="mt-2 text-slate-500">Make your private space feel like yours.</p><section className="card mt-7"><div className="flex items-center gap-3"><Palette className="text-violet-500"/><h2 className="font-display text-2xl font-bold">Choose a theme</h2></div><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{themes.map(([id,label,cls])=><button key={id} onClick={()=>setTheme(id)} className={`rounded-3xl border p-5 text-left ${theme===id?'border-[var(--cn-accent)] ring-4 ring-[var(--cn-soft)]':'border-slate-200'} ${cls}`}><div className="flex items-center justify-between"><span className="grid size-10 place-items-center rounded-2xl bg-white/70"><Sparkles size={18}/></span>{theme===id&&<span className="text-xs font-bold text-[var(--cn-accent)]">Selected</span>}</div><b className="mt-5 block">{label}</b><span className="mt-1 block text-xs opacity-70">Live preview theme</span></button>)}</div></section><section className="card mt-5"><div className="flex items-center justify-between gap-4"><div><h2 className="font-display text-2xl font-bold">Animations</h2><p className="mt-1 text-sm text-slate-500">Keep gentle hover and entrance animations enabled.</p></div><button onClick={()=>setMotion(!motion)} className={`relative h-7 w-12 rounded-full ${motion?'bg-[var(--cn-accent)]':'bg-slate-300'}`}><span className={`absolute top-1 size-5 rounded-full bg-white transition ${motion?'left-6':'left-1'}`}/></button></div></section></div>}
